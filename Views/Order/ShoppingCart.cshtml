@model MovieShopDelta.Models.Database.Customer

@{
    ViewBag.Title = "ShoppingCart";
}

<h2>Shopping Cart</h2>
<hr />
<br />

@Html.Action("BoughtMovies")

<br />
<br />
@if (ViewBag.HasMovieId == true)
{
using (Html.BeginForm("CheckOut", "Order", FormMethod.Post))
{
    <div class="row" style="margin-left: 2px">
        @Html.AntiForgeryToken()
        @Html.EditorFor(model => model.EmailAddress, new { htmlAttributes = new { @class = "form-control", placeholder = "email@example.com", style = "max-width: 280px" } })
        &ensp;
        <button type="submit" value="create" class="btn btn-success" style="width:80px">Create</button>
    </div>
    <p class="text-muted" style="margin-left: 4px; margin-top: 4px"><i>Enter email address to place the order</i></p>

    @*@Html.EditorFor(u => u.EmailAddress, new { htmlAttributes = new { placeholder = "email@example.com" } })
        <input class="btn btn-success" type="submit" value="Submit" />*@
}
}

